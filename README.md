# Subtext Market Data Dashboard

Internal market-data dashboard for Subtext Living. Static HTML + JS + CSS,
no backend. Data is exported nightly/weekly from Azure SQL into `data.json`,
and the site reads that file on page load.

**Hosted at:** _(TBD — enable GitHub Pages in repo settings)_

---

## Stack

- Pure static — `index.html`, `market.html`, `property.html` + plain CSS + plain JS
- Leaflet for the maps (multiple basemap layers, fullscreen, click-to-drill)
- Chart.js for the bar / line charts on the Industry tab
- Subtext masterbrand fonts (Mencken Std + Pragmatica) self-hosted under `assets/fonts/`

---

## Repo layout

```
.
├── index.html              Industry view — national map, KPIs, scorecard table, charts
├── market.html             Market detail — KPIs, polygon map, comp properties, qualifier scorecard
├── property.html           Property detail — KPIs, mini-map, floor plans
├── dashboard.js            JS for index.html (Industry + Analysis + Sources tabs)
├── market.js               JS for market.html
├── property.js             JS for property.html
├── style.css               All styling, shared across pages
├── data.json               Master dataset (refreshed by export-data.py)
├── plans/<property>.json   Per-property floor plans (lazy-loaded by property.html)
├── assets/
│   ├── fonts/              Mencken Std + Pragmatica (brand fonts)
│   ├── subtext-*.png       Brand logos
│   ├── campus-logos/       University logos for the Subtext-30
│   └── campus-boundaries/  GeoJSON campus footprints (29 universities)
├── export-data.py          Runs the SQL queries → writes data.json + plans/
├── fetch-campus-logos.py   One-off: pulls Wikipedia logos for the Subtext-30
└── fetch-campus-boundaries.py  One-off: pulls OSM campus polygons
```

---

## Refresh the data

The site is static — `data.json` is regenerated by running the export script
against the Azure SQL database. Requires:

- ODBC Driver 18 for SQL Server
- Python 3.10+ with `pyodbc` and `openpyxl`
- A user with `db_datareader` on `StudentResearch`
- The Market Analysis Schedule Excel synced locally via OneDrive
  (`General/Markets/Support/Market Analysis Schedule.xlsx`)

```bash
# Set credentials in env vars — never commit them
set SQLUSER=<your_user>
set SQLPASSWORD=<your_password>

python export-data.py
```

The script writes:
- `data.json` (~7 MB, all tables for the Industry, Market, and Analysis tabs)
- `plans/<property_key>.json` (~3,600 small files for lazy-loaded floor plans)

Push the updated files to redeploy.

---

## Run locally

```bash
python -m http.server 8765
```

Open `http://127.0.0.1:8765/`. No build step.

---

## Deploy to GitHub Pages

1. Repo Settings → Pages → Source: deploy from a branch → `main` / root
2. Save. Site goes live at `https://jmillenbrucksubtext.github.io/Market-Data-Dashboard-Version-1/` in ~1 minute.

---

## Auto-refresh (optional, future)

GitHub Actions could run `export-data.py` on a weekly cron with the SQL
credentials stored as repo secrets. Caveat: the Market Analysis Schedule
Excel lives on the user's OneDrive and isn't reachable from an Actions
runner — that section of `data.json` would need to be preserved from the
existing file, or fetched via Microsoft Graph API (separate setup).

---

## Sensitivity

This is a **public** repo. Anyone with the URL can read every file in every
commit, forever. The current `data.json` includes:

- Licensed market data (rents, occupancy, prelease from third-party providers)
- The Subtext-30 focus list and anchor universities
- Internal pipeline tracker (analyst initials, IC dates, decisions) from the
  Market Analysis Schedule Excel

Before promoting this URL widely, decide whether an access gate is needed.
Free option: Cloudflare Pages + Cloudflare Access (email-based, ≤50 users).
